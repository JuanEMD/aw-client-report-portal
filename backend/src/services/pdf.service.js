// TODO: Implement PDF generation
// Option A: Puppeteer (HTML+SVG → PDF)
// Option B: pdfkit (programmatic drawing)

function generateSACS(reportData, clientData, sacsData) {
  // STUB — returns placeholder
  return Buffer.from('SACS PDF placeholder');
}

function generateTCC(reportData, clientData, tccData) {
  // STUB — returns placeholder
  return Buffer.from('TCC PDF placeholder');
}

module.exports = { generateSACS, generateTCC };
